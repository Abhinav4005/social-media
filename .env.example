# Example environment variables for the entire project
# Copy this file and rename to .env in the respective directories

# ============================================
# BACKEND ENVIRONMENT VARIABLES
# Location: backend/.env
# ============================================

# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/socialhub_db

# JWT Secret (use a strong random string)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:5173

# Email Configuration (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password
FROM_EMAIL="Social Hub <noreply@socialhub.com>"

# Redis (Upstash - get free account at upstash.com)
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-token

# ImageKit (get free account at imagekit.io)
IMAGEKIT_PUBLIC_KEY=public_your_public_key
IMAGEKIT_PRIVATE_KEY=private_your_private_key
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_imagekit_id

# Node Environment
NODE_ENV=development

# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# Location: frontend/.env
# ============================================

# API Base URL
VITE_API_URL=http://localhost:3000/api/v1

# Socket.io URL (usually same as backend)
VITE_SOCKET_URL=http://localhost:3000
